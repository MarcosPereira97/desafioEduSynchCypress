{
  "devDependencies": {
    "@shelex/cypress-allure-plugin": "^2.37.0",
    "cypress": "^12.8.1"
  },
  "dependencies": {
    "cypress-browser-permissions": "^1.1.0"
  },
  "scripts": {
    "npm:headless": "npx cypress run --env allure=true",
    "npm:open": "npx cypress open --env allure=true",
    "yarn:headless": "yarn cypress run --env allure=true",
    "yarn:open": "yarn cypress open --env allure=true"
  },
  "name": "cypressChallenge",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Marcos Pereira",
  "license": "MIT"
}
